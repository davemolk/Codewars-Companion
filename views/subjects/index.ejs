<ul>
  <% subjects.forEach((s, index) => { %>
  <li><%= s.name %> (id is <%= s.id %>)</li>
  <form method="get" action="/subjects/edit/<%= s.id %> ">
    <button type="submit" class="btn btn-secondary">edit</button>
  </form>

  <% }) %>
</ul>

<br />

<a href="/">&larr; Back Home</a>
